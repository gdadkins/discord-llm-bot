# Gemini Vision Configuration for Maximum Accuracy
# Add these to your .env file for best character recognition

# Use the HIGH_ACCURACY_VISION profile
GEMINI_VISION_PROFILE=HIGH_ACCURACY_VISION

# Model configuration (when you have access to better models)
# GEMINI_MODEL=gemini-1.5-pro-vision  # Best accuracy but requires paid access
GEMINI_MODEL=gemini-2.5-flash-preview-05-20  # Current free model

# Generation parameters for maximum accuracy
GEMINI_TEMPERATURE=0.05  # Even lower than 0.1 for more deterministic results
GEMINI_TOP_K=5          # Very narrow selection (was 10, now 5)
GEMINI_TOP_P=0.7        # Tighter focus on high-probability answers (was 0.8)
GEMINI_MAX_TOKENS=4096  # Increase to allow more detailed analysis

# Optional: Add penalties to reduce repetition
GEMINI_PRESENCE_PENALTY=0.0
GEMINI_FREQUENCY_PENALTY=0.0

# System instruction override for SNL-specific recognition
GEMINI_SYSTEM_INSTRUCTION="You are an expert at recognizing Saturday Night Live (SNL) characters, cast members, and sketches. When analyzing images: 1) Focus on identifying specific SNL characters, recurring sketches, and cast members from all seasons. 2) Pay attention to costumes, wigs, makeup, and character-specific props. 3) Consider the era/season based on visual style and set design. 4) Reference specific sketch names and character names when possible. 5) If you see a character that looks familiar but you're unsure, provide your top 2-3 possibilities with reasoning. 6) Note distinctive features like catchphrases visible in text or iconic poses. You have extensive knowledge of SNL history from 1975 to present."